import { createApp } from 'vue'
import App from './components/App.vue'
// import Sky from "./components/Sky.vue"
import A from "./components/A.vue"

// createApp(App).mount('#app')
// createApp(Sky).mount('#app')
createApp(A).mount('#app')
